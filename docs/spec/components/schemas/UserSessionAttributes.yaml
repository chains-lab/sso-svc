type: object
required:
  - user_id
  - created_at
  - last_used
properties:
  user_id:
    type: string
    format: uuid
    description: "user id"
  created_at:
    type: string
    format: date-time
    description: "session creation date"
  last_used:
    type: string
    format: date-time
    description: "last used date"

